# 计算器程序规格说明书（SDD）

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 双数四则运算计算器规格说明书 |
| 文档版本 | 1.0 |
| 编写日期 | 2026-08-18 |
| 文档类型 | 软件需求规格说明书（SRS / SDD） |
| 目标平台 | Windows 桌面应用 |

---

## 2. 项目概述

### 2.1 项目背景

开发一款带图形界面的 Windows 桌面计算器程序，用于完成两个数的加、减、乘、除四则运算。

### 2.2 项目目标

- 提供直观、易用的图形界面，方便用户进行两个数的四则运算。
- 支持按键输入与键盘键入两种交互方式。
- 支持小数与负数输入，结果准确展示。

### 2.3 术语定义

| 术语 | 定义 |
| --- | --- |
| 显示屏 | 界面中用于显示当前输入与结果的区域 |
| 第一操作数 / 第二操作数 | 参与运算的两个数值 |
| 当前操作符 | 当前选中的 +、-、×、÷ 之一 |

---

## 3. 功能需求

### 3.1 基本运算功能

- FR-01：程序应支持加法（+）、减法（-）、乘法（×）、除法（÷）四种运算。
- FR-02：每次运算应恰好由两个操作数和一个操作符组成，例如 `12 + 7`。
- FR-03：运算结果应准确显示在显示屏上。

### 3.2 数值输入

- FR-04：支持输入整数与小数（小数点 .）。
- FR-05：支持负数输入（可通过符号键输入负号）。
- FR-06：输入长度超过显示屏容量时应给出提示，并拒绝继续输入。
- FR-07：输入小数时，同一个小数中只允许出现一个小数点。

### 3.3 运算流程

- FR-08：用户依次输入第一操作数 → 选择操作符 → 输入第二操作数 → 点击等号（=）得出结果。
- FR-09：输入过程中，用户可随时修改当前操作数（退格或重新输入）。
- FR-10：在尚未输入第二操作数时再次点击操作符，应替换当前操作符。
- FR-11：连续运算时，上一步的结果可作为下一次运算的第一操作数继续使用。

### 3.4 清除与修正

- FR-12：提供清空（C）功能，一键清除当前所有输入并重置状态。
- FR-13：提供退格功能，删除当前输入的最后一位字符。

### 3.5 除零处理

- FR-14：当除数为 0 时，点击等号后应在显示屏上显示错误提示（如 `Error: Division by zero`）。
- FR-15：出现除零错误后，程序应进入错误状态，用户可通过清空（C）按钮恢复正常使用。

### 3.6 输入交互

- FR-16：支持通过鼠标点击界面按钮完成全部操作。
- FR-17：支持通过物理键盘输入数字、小数点、负号、操作符、回车（等号）、退格与清除键（Esc / C）。
- FR-18：按键输入与键盘输入应实时同步显示在显示屏上。

---

## 4. 非功能需求

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| NFR-01 | 易用性 | 所有功能可通过鼠标点击完成，主要操作可通过键盘完成 |
| NFR-02 | 准确性 | 运算结果误差仅限浮点数表示精度的正常误差，不允许逻辑错误 |
| NFR-03 | 响应性 | 所有按钮点击与键盘输入的响应时间应小于 100ms，无卡顿 |
| NFR-04 | 稳定性 | 程序不得崩溃；所有非法输入（如连续小数点、除零）应被妥善处理 |
| NFR-05 | 可移植性 | 程序应在 Windows 10 及以上版本正常运行 |

---

## 5. 界面 UI 设计

### 5.1 整体布局

界面采用单窗口布局，由上至下分为两个区域：

1. **显示屏区域**：位于窗口顶部，右对齐显示当前输入、表达式或结果。
2. **按键区域**：位于显示屏下方，由 5 行按键组成。

### 5.2 显示屏设计

| 项目 | 规格 |
| --- | --- |
| 位置 | 窗口顶部 |
| 对齐方式 | 内容右对齐 |
| 显示内容 | 当前输入的数字、或运算结果、或错误提示 |
| 最大显示长度 | 不超过 20 个字符，超长时提示并拒绝输入 |
| 显示格式 | 结果按普通十进制显示，避免科学计数法（除非数值极大/极小） |

### 5.3 按键布局

界面采用 5 行 4 列按键网格（共 19 个功能按键，其中退格键跨两格），建议布局如下：

| 第一行 | <td colspan="2">← (退格)</td> | C | ÷ |
| --- | --- | --- | --- |
| **第二行** | 7 | 8 | 9 | × |
| **第三行** | 4 | 5 | 6 | - |
| **第四行** | 1 | 2 | 3 | + |
| **第五行** | ± (负号) | 0 | . | = |

注：退格键（←）位于第一行左侧，跨第 1、2 两格；C 键位于第一行第 3 格；÷ 键位于第一行第 4 格，其正下方即乘号键（×）所在列。

按键说明：

| 按键 | 功能 |
| --- | --- |
| C | 清空所有输入，重置状态 |
| ← | 退格，删除当前输入的最后一位 |
| ± (负号) | 切换当前操作数的正负号 |
| ÷ / × / - / + | 选择运算操作符 |
| 0-9 | 输入数字 |
| . | 输入小数点 |
| = | 计算结果 |

### 5.4 视觉设计

| 项目 | 规格 |
| --- | --- |
| 窗口尺寸 | 固定大小，不可拉伸（约 300×420 像素） |
| 配色 | 简洁浅色主题；数字键为浅色，操作符键与 C 键为强调色 |
| 字体 | 显示屏使用大号等宽字体，按键使用清晰易读的常规字体 |
| 语言 | 界面文字使用英文（C、←、±、=、+、-、×、÷） |
| 错误提示 | 除零或输入错误时，显示屏显示红色错误文字（如 `Error: Division by zero`） |

### 5.5 交互反馈

- 按钮按下时应有一致的视觉反馈（如颜色变化或凹陷效果）。
- 出现除零错误后程序进入错误状态，此时仅 C 键有效；按 C 后显示屏清空，恢复正常输入。

---

## 6. 验收标准

### 6.1 功能验收

| 编号 | 验收项 | 操作步骤 | 预期结果 |
| --- | --- | --- | --- |
| AC-01 | 加法 | 输入 12 + 7 = | 显示 19 |
| AC-02 | 减法 | 输入 20 - 5 = | 显示 15 |
| AC-03 | 乘法 | 输入 6 × 7 = | 显示 42 |
| AC-04 | 除法 | 输入 100 ÷ 4 = | 显示 25 |
| AC-05 | 小数运算 | 输入 3.5 + 2.25 = | 显示 5.75 |
| AC-06 | 负数运算 | 输入 -5 + 3 = | 显示 -2 |
| AC-07 | 除零错误 | 输入 10 ÷ 0 = | 显示 Error: Division by zero 提示 |
| AC-08 | 错误恢复 | 出现除零错误后点击 C | 显示屏清空，恢复正常状态 |
| AC-09 | 清空功能 | 输入任意内容后点击 C | 显示屏清空，所有状态重置 |
| AC-10 | 退格功能 | 输入 123 后点击退格 | 显示屏显示 12 |
| AC-11 | 替换操作符 | 输入 5 + × 再输入 3 = | 结果为 15（使用 × 运算） |
| AC-12 | 连续运算 | 计算 2 + 3 = 后再输入 × 4 = | 显示 20 |
| AC-13 | 小数点限制 | 输入 1.2.3 | 第二个小数点被拒绝，显示 1.23 |
| AC-14 | 超长输入限制 | 连续输入超过 20 个字符 | 拒绝继续输入并提示 |

### 6.2 交互验收

| 编号 | 验收项 | 预期结果 |
| --- | --- | --- |
| AC-15 | 鼠标操作 | 全部功能可通过鼠标点击完成 |
| AC-16 | 键盘操作 | 数字键、小数点、负号、+ - * /、回车、退格、Esc（或 C）均可操作 |
| AC-17 | 显示同步 | 按键输入与键盘输入在显示屏上显示一致 |

### 6.3 非功能验收

| 编号 | 验收项 | 预期结果 |
| --- | --- | --- |
| AC-18 | 响应时间 | 所有操作的界面响应时间小于 100ms |
| AC-19 | 稳定性 | 由自动化测试脚本连续执行 100 次随机四则运算，程序不崩溃、无逻辑错误 |
| AC-20 | 运行环境 | 在 Windows 10 / Windows 11 上均能正常启动与使用 |

### 6.4 验收方式

以上验收项全部通过，即视为本规格说明书验收合格。

---

## 7. 范围说明（不在本次范围内）

- 不实现科学计算功能（平方根、幂、三角函数等）。
- 不实现计算历史记录。
- 不实现百分号（%）等附加辅助按键（± 负号键除外，它属于负数输入的核心功能，见 FR-05）。
- 不实现多操作数混合表达式（如 `1 + 2 × 3`）。
- 不实现内存存储（M+、MR 等）。
