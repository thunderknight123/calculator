# 计算器程序任务清单（task.md）

## 0. 说明

- 依据：plan.md 各阶段（下文以 **PX** 表示 plan.md 阶段 X）与 spec.md 验收项（以 **AC-XX** 表示 spec.md §6 中的编号）。
- 任务状态：`待办` / `进行中` / `已完成`。
- 目录约定：项目根目录 `D:\code\calculator`，源码位于 `src\`，测试位于 `tests\`。

### 项目结构规划

| 路径 | 职责 |
| --- | --- |
| `src\main.c` | 入口：窗口初始化与主循环 |
| `src\calc\calc.h` / `src\calc\calc.c` | 逻辑层：输入状态机、运算核心、错误状态（不依赖 raylib） |
| `src\ui\ui.h` / `src\ui\ui.c` | 界面层：显示屏渲染、按键网格、键盘事件 |
| `tests\test_input.c` / `tests\test_ops.c` | 逻辑层单元测试 |
| `tests\test_random.c` | 随机四则运算稳定性测试 |
| `CMakeLists.txt`（或 Makefile） | 构建脚本 |

---

## 1. 任务总览

| 任务 ID | 名称 | 涉及文件 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| T0-1 | 编译环境准备 | 无（系统环境） | — | 已完成 |
| T0-2 | 项目骨架与构建脚本 | `CMakeLists.txt`（或 Makefile）、`src\` 与 `tests\` 目录 | T0-1 | 已完成 |
| T0-3 | 固定窗口与主循环 | `src\main.c` | T0-2 | 已完成 |
| T1-1 | 显示屏渲染模块 | `src\ui\ui.h`、`src\ui\ui.c` | T0-2 | 已完成 |
| T1-2 | 显示屏接入主循环 | `src\main.c`、`src\ui\ui.c` | T0-3、T1-1 | 已完成 |
| T2-1 | 输入状态机实现 | `src\calc\calc.h`、`src\calc\calc.c` | T0-2 | 待办 |
| T2-2 | 输入逻辑单元测试 | `tests\test_input.c` | T2-1 | 待办 |
| T3-1 | 运算核心状态机 | `src\calc\calc.h`、`src\calc\calc.c` | T2-1 | 待办 |
| T3-2 | 结果格式化 | `src\calc\calc.c` | T3-1 | 待办 |
| T3-3 | 运算逻辑单元测试 | `tests\test_ops.c` | T3-2 | 待办 |
| T4-1 | 按键网格布局 | `src\ui\ui.c` | T1-1 | 待办 |
| T4-2 | 命中检测与视觉反馈 | `src\ui\ui.c` | T4-1 | 待办 |
| T4-3 | 按键接入逻辑层 | `src\ui\ui.c`、`src\calc\calc.h` | T4-2、T3-1 | 待办 |
| T5-1 | 键盘映射与共享输入入口 | `src\ui\ui.c`、`src\main.c` | T4-3、T3-1 | 待办 |
| T5-2 | 双输入显示同步验证 | `src\main.c` | T5-1 | 待办 |
| T6-1 | 错误状态输入门禁 | `src\calc\calc.c`、`src\ui\ui.c` | T3-1、T4-3 | 待办 |
| T6-2 | 错误显示与恢复流程 | `src\ui\ui.c`、`src\main.c` | T6-1、T1-2 | 待办 |
| T7-1 | 随机四则运算自动化测试 | `tests\test_random.c` | T3-3 | 待办 |
| T7-2 | 全量验收走查 | 全部源码 | T4-3、T5-2、T6-2 | 待办 |
| T7-3 | 性能与兼容性检查 | 构建产物 | T7-2 | 待办 |

---

## 2. 任务明细与检查点

### 阶段 0 任务（参照 P0）

**T0-1 编译环境准备**
- 内容：安装 raylib 并验证编译链（编译器、链接库路径）。
- 检查点摘要：空项目可编译链接 raylib 并生成可执行文件。

**T0-2 项目骨架与构建脚本**
- 内容：建立 `src\calc`、`src\ui`、`tests` 目录；创建构建脚本，配置头文件包含与链接选项（保证逻辑层不引入 raylib 依赖）。各模块头文件由对应功能任务创建（ui.h 见 T1-1，calc.h 见 T2-1）。
- 检查点摘要：构建脚本一键完成编译；逻辑层编译时不引用 raylib 头文件。

**T0-3 固定窗口与主循环**
- 内容：创建固定尺寸窗口（参照 P0：尺寸遵循 spec.md §5.4），启动主循环，处理窗口关闭事件。
- 检查点摘要：窗口可打开、尺寸固定、可正常关闭（参照 AC-20 启动环节）；界面为英文（参照 spec.md §5.4）。

### 阶段 1 任务（参照 P1）

**T1-1 显示屏渲染模块**
- 内容：实现显示屏区域渲染——右对齐文本（先测量文本宽度再定位）、大号等宽字体、三种渲染状态（输入中/结果/错误）切换、错误状态使用红色文字（参照 spec.md §5.2、§5.4）。
- 检查点摘要：20 字符以内文本右对齐显示；超长时按 AC-14 显示提示效果；错误文字为红色。

**T1-2 显示屏接入主循环**
- 内容：将显示屏模块挂入主循环，并接入输入/结果/错误三种状态的数据来源。
- 检查点摘要：主循环中显示屏内容随状态切换正确刷新。

### 阶段 2 任务（参照 P2）

**T2-1 输入状态机实现**
- 内容：操作数字符串维护与数值转换——数字追加、小数点单次限制（参照 AC-13）、± 正负切换（参照 AC-06）、退格（参照 AC-10）、20 字符上限拒绝（参照 AC-14）、重新输入覆盖（参照 P2 要点）。
- 检查点摘要：非法输入（连续小数点）被拒绝；超长输入被拒绝；± 与退格行为符合上述 AC 预期。

**T2-2 输入逻辑单元测试**
- 内容：为输入状态机编写测试用例（数字追加、小数点、退格、正负切换、长度上限）。
- 检查点摘要：测试全部通过，对应 AC-10、AC-13、AC-14 的输入场景。

### 阶段 3 任务（参照 P3）

**T3-1 运算核心状态机**
- 内容：实现双操作数状态机（空闲→第一操作数→已选操作符→第二操作数→结果）、结果续算（参照 AC-12）、操作符替换（参照 AC-11）、除零检测进入错误状态（参照 FR-14）。
- 检查点摘要：逻辑层四则运算结果正确（参照 AC-01~AC-06）；除零触发错误状态标记（错误显示与 C 键恢复的完整验收见 T6-2，参照 AC-07、AC-08）。

**T3-2 结果格式化**
- 内容：结果按普通十进制输出，避免科学计数法（数值极大/极小除外），浮点误差控制在正常范围（参照 spec.md §5.2、NFR-02）。
- 检查点摘要：典型小数运算结果格式正确（参照 AC-05）。

**T3-3 运算逻辑单元测试**
- 内容：为运算核心编写测试（加减乘除、小数、负数、操作符替换、连续运算、除零）。
- 检查点摘要：测试全部通过，对应 AC-01~AC-06、AC-11、AC-12。

### 阶段 4 任务（参照 P4）

**T4-1 按键网格布局**
- 内容：按 spec.md §5.3 布局实现 5 行 4 列按键网格——退格键跨前两格、C 键在第一行第 3 格、÷ 键在乘号正上方、± 键在左下角。
- 检查点摘要：按键位置与布局表逐格一致（退格占两格）。

**T4-2 命中检测与视觉反馈**
- 内容：实现按键矩形命中检测、按下视觉反馈（颜色变化）、配色区分（数字键浅色，操作符键与 C 键强调色，参照 spec.md §5.4、§5.5）。
- 检查点摘要：鼠标点击任意键位均能命中且反馈一致。

**T4-3 按键接入逻辑层**
- 内容：将各按键接入逻辑层接口——数字/小数点/± 走输入状态机，操作符走运算核心，C 触发清空，= 触发计算。
- 检查点摘要：鼠标可完成全部操作（参照 AC-15）；= 计算结果、C 清空符合 AC-09。

### 阶段 5 任务（参照 P5）

**T5-1 键盘映射与共享输入入口**
- 内容：实现键盘映射（数字、小数点、负号、+ - * /、回车=、退格、Esc/C，参照 AC-16），与鼠标共用同一输入处理入口。
- 检查点摘要：键盘全部映射键可操作（参照 AC-16）；输入入口与鼠标路径一致。

**T5-2 双输入显示同步验证**
- 内容：验证鼠标与键盘输入在显示屏上显示一致。
- 检查点摘要：同一操作序列经鼠标与键盘输入，显示屏结果一致（参照 AC-17）。

### 阶段 6 任务（参照 P6）

**T6-1 错误状态输入门禁**
- 内容：错误状态下仅 C 键有效，其余输入（数字、操作符、退格、±）被忽略（参照 spec.md §5.5）。
- 检查点摘要：错误状态下非 C 输入均无效。

**T6-2 错误显示与恢复流程**
- 内容：错误提示以红色文字显示（参照 AC-07）；按 C 后显示屏清空、全部状态重置（参照 AC-08）。
- 检查点摘要：除零操作显示错误提示；按 C 恢复后行为与 AC-08 一致。

### 阶段 7 任务（参照 P7）

**T7-1 随机四则运算自动化测试**
- 内容：编写随机测试——随机生成操作数与操作符，连续执行 100 次四则运算并与参考结果比对（参照 AC-19）。
- 检查点摘要：100 次随机运算全部通过，无崩溃、无逻辑错误。

**T7-2 全量验收走查**
- 内容：按 spec.md §6 全部验收项逐条人工走查（功能、交互、非功能），发现问题后修复并回归。
- 检查点摘要：AC-01~AC-20 全部通过。

**T7-3 性能与兼容性检查**
- 内容：测量操作响应时间；在 Windows 10 / 11 环境验证运行。
- 检查点摘要：响应时间小于 100ms（参照 AC-18）；Windows 10/11 正常启动使用（参照 AC-20）。

---

## 3. 里程碑

| 里程碑 | 达成条件 | 涉及任务 |
| --- | --- | --- |
| M1 项目可运行 | 窗口正常打开关闭 | T0-1 ~ T0-3 |
| M2 逻辑层完成 | 输入与运算单测全通过 | T2-1 ~ T3-3 |
| M3 界面完成 | 鼠标键盘可完成全部操作 | T4-1 ~ T5-2 |
| M4 错误处理完成 | 除零错误与恢复符合验收 | T6-1 ~ T6-2 |
| M5 验收通过 | 全量验收走查无遗留问题 | T7-1 ~ T7-3 |
