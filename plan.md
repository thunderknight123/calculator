# 计算器程序执行规划（plan.md）

## 0. 规划概述

| 项目 | 内容 |
| --- | --- |
| 依据文档 | spec.md v1.0 |
| 技术栈 | C 语言 + raylib（已确认） |
| 开发方式 | 逻辑层与界面层分离，便于单元测试 |

### 0.1 模块划分

- **逻辑层**（calc）：输入状态机、运算核心、错误状态，不依赖 raylib，可独立测试。
- **界面层**（ui）：窗口、显示屏、按键网格、键盘事件，调用逻辑层接口。
- **入口**（main）：初始化窗口与主循环。

### 0.2 阶段总览（按依赖顺序）

| 阶段 | 名称 | 依赖 |
| --- | --- | --- |
| 阶段 0 | 环境搭建与项目骨架 | — |
| 阶段 1 | 显示屏模块 | 阶段 0 |
| 阶段 2 | 输入状态机（数值层） | 阶段 0 |
| 阶段 3 | 运算核心（双操作数状态机） | 阶段 2 |
| 阶段 4 | 按键网格界面 | 阶段 1、2、3 |
| 阶段 5 | 键盘输入支持 | 阶段 1、2、3 |
| 阶段 6 | 错误状态与恢复 | 阶段 1、3、4 |
| 阶段 7 | 集成测试与验收 | 阶段 1~6 |

---

## 阶段 0：环境搭建与项目骨架

### 实现要点

- 安装并验证 raylib 环境（选择编译器链并确定 raylib 的获取与链接方式）。
- 建立项目目录结构与构建脚本（CMake 或 Makefile），保证一键编译运行。
- 创建固定尺寸窗口并启动主循环：`InitWindow` 固定窗口（尺寸参照 spec.md §5.4）、目标帧率设置、窗口关闭事件处理。
- 建立逻辑层（不包含 raylib 头文件）与界面层的代码分层。

### 完成标准

- 编译脚本可用，窗口能正常打开与关闭，界面为英文（spec.md §5.4 语言要求）。

---

## 阶段 1：显示屏模块

### 实现要点

- 显示屏渲染：顶部区域、内容右对齐（右对齐需先测量文本宽度再定位，spec.md §5.2）。
- 双行显示：显示屏顶部以小字号显示上一次计算过程（如 `12 + 7 =`），下方以大字号显示当前输入/结果/错误提示（spec.md §5.2 运算过程显示）。
- 大号等宽字体渲染；错误提示使用红色文字（spec.md §5.4）。
- 显示缓冲区容量限制：最多 20 个字符，超长时显示提示（spec.md §5.2、FR-06）。
- 预留三种渲染状态：输入中、结果、错误。

### 完成标准

- 超长输入时显示屏的提示效果符合 spec.md §6.1 AC-14 的预期。

---

## 阶段 2：输入状态机（数值层）

### 实现要点

- 纯逻辑层实现，不依赖 raylib，保证可单元测试。
- 当前操作数的字符串维护与数值转换：数字追加、小数点输入（同一个小数仅允许一个点，spec.md §3.2 FR-07）。
- 重新输入覆盖：选中操作符后输入新数字时，应清空旧操作数、从新数字开始接收，而非追加（FR-09）。
- 正负号切换：± 键对当前操作数切换正负（FR-05）。
- 退格：删除当前输入最后一位（FR-13）。
- 长度上限校验与拒绝输入（FR-06）。
- 非法输入（连续小数点等）的防御性处理（NFR-04）。

### 完成标准

- 输入场景符合 spec.md §6.1 AC-10、AC-13、AC-14 的预期。

---

## 阶段 3：运算核心（双操作数状态机）

### 实现要点

- 状态机建模：空闲 → 第一操作数 → 已选操作符 → 第二操作数 → 结果；结果可作为下次运算的第一操作数（spec.md §3.3 FR-08、FR-11）。
- 操作符替换：未输入第二操作数时再次选择操作符，替换当前操作符（FR-10）。
- 除零检测：除数为 0 时进入错误状态（FR-14、FR-15）。
- 计算过程记录：等号计算时生成"操作数 操作符 操作数 ="文本（如 `12 + 7 =`），供显示屏顶部小字号显示（spec.md §5.2 运算过程显示）。
- 结果格式化：普通十进制显示，避免科学计数法（§5.2、NFR-02）；浮点精度误差需控制在正常范围内。

### 完成标准

- 运算结果符合 spec.md §6.1 AC-01~AC-06、AC-11、AC-12 的预期（AC-07、AC-08 涉及界面显示与 C 键操作，归阶段 6 验收）。

---

## 阶段 4：按键网格界面

### 实现要点

- 按 spec.md §5.3 的布局表实现 5 行 4 列按键网格：退格键跨前两格、± 键在左下角、C 键在第一行第 3 格、÷ 键在乘号正上方等。
- 按键命中检测（矩形区域判定）与按下时的视觉反馈（spec.md §5.5）。
- 配色：数字键浅色，操作符键与 C 键为强调色（§5.4）。
- 将各按键接入输入状态机与运算核心（= 触发计算，C 触发清空）。

### 完成标准

- 鼠标可完成全部操作，符合 spec.md §6.2 AC-15。

---

## 阶段 5：键盘输入支持

### 实现要点

- 键盘映射（spec.md §3.6 FR-17）：数字键、小数点、负号、+ - * /、回车（等号）、退格、Esc/C。
- 键盘与鼠标共用同一输入处理入口，保证两种输入在显示屏上显示一致（FR-18）。

### 完成标准

- 键盘操作符合 spec.md §6.2 AC-16、AC-17。

---

## 阶段 6：错误状态与恢复

### 实现要点

- 错误状态下仅 C 键有效，其余按键输入被忽略（spec.md §5.5）。
- 按 C 后清空显示屏并重置全部状态（FR-15）。
- 错误提示以红色文字在显示屏呈现（§5.4）。

### 完成标准

- 错误状态相关场景符合 spec.md §6.1 AC-07、AC-08。

---

## 阶段 7：集成测试与验收

### 实现要点

- 逻辑层自动化测试：随机生成操作数与运算符，连续执行 100 次四则运算并与参考结果比对（spec.md §6.3 AC-19）。
- 按 spec.md §6 的验收项逐条人工走查，修复发现的问题并回归。
- 性能检查：操作响应时间小于 100ms（AC-18）；在 Windows 10 / 11 上验证可运行（AC-20）。

### 完成标准

- spec.md §6 中 AC-01 ~ AC-20 全部通过。

---

## 附：风险与注意事项

- raylib 默认字体仅支持 ASCII 字符，本程序为英文界面，可满足要求。
- C 浮点运算存在精度误差，结果展示需按 spec.md §5.2 格式化，避免误判为逻辑错误。
- 键盘按键需处理连续按压与释放事件，避免一次按压触发多次输入（若规格要求如此，见 FR-17）。
- 阶段 4 与阶段 5 共享输入处理逻辑，应先完成阶段 3 再并行开展，降低返工风险。
